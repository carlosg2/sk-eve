---
cron: "0 7 * * *"
---

Resumen de estado del agente: responde en máximo dos líneas con un resumen ejecutivo del estado del sistema (sin inventar datos del ERP). Tarea demo para validar el sistema de schedules de Eve — `eve dev` nunca la dispara en cadencia; se ejecuta con el botón "Probar" de la pantalla Schedules en /studio (o la ruta dev de dispatch).
